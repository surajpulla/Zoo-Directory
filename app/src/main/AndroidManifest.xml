<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.suraj.zoodirectory">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/zooicon1"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".zooinfo"
            android:label="@string/title_activity_zooinfo"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".individual_animal"
            android:label="@string/title_activity_individual_animal"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>
